---
description: Show the Hydrate Triad Workflow Guide and full CLI command reference.
---

Run `hydrate help` in the project root using the Bash tool, then summarize the
printed Triad Workflow Guide (the 6-step boot sequence across Product Owner,
Lead Architect, and Lead Developer) and the available `hydrate` commands.
