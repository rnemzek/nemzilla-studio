---
description: Print a plain-English map of .hydrate/ and .claude/commands/ plus .gitignore guidance.
---

Run `hydrate artifacts` in the project root using the Bash tool, then summarize
the artifact tree and the recommended `.gitignore` entries for the Product Owner.
