---
description: Run the Hydrate state reconciler to orient on the active UOW, archive, and git status.
---

Run `hydrate checkup` in the project root using the Bash tool, then summarize the
reported session state (clean-slate / in-progress / unarchived-done / broken-build)
and the next recommended action for the Product Owner.
