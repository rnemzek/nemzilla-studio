---
description: Inspect Hydrate UOWs — list all, show the last completed, or show one by ID.
---

Run `hydrate uow $ARGUMENTS` in the project root using the Bash tool (with no
arguments this defaults to `hydrate uow list`), then present the result to the
Product Owner.
