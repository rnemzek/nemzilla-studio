---
description: Output a token-dense Hydrate context prompt and copy it to the clipboard.
---

Run `hydrate context --clip` in the project root using the Bash tool, then use
the printed context payload to reorient on the active scope, recent
architectural decisions, and macro roadmap before continuing.
